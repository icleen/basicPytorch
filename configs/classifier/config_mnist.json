{
  "task": "classifier",
  "type": "mnist",

  "batch_size": 40,
  "vbatch_size": 40,
  "model_def": "configs/{}/mnist.cfg",

  "data_config": "/scratch/mnist/",

  "learning_rate": 0.0001,
  "n_cpu": 8,
  "img_size": 28,
  "checkpoint_interval": 1,
  "evaluation_interval": 1,
  "epochs": 40,

  "checkpoint_path": "checkpoints/",
  "log_path": "{}_logs",
  "val_metrics": "{}_metrics.txt"

}
